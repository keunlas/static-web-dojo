# 43 · 基础篇 34 式（字符串、正则、数字与日期）

> 来源：2026-09-14 同一轮工作（基础篇改版的第二十六批次）。正文原样归档，编号只增不改。

## 本批完成的事

**`site/basics/34-js-strings-numbers.html`**（4 个演示 + 3 道练功，本节偏“字典”体量）：

- 字符串方法速查表（length / includes / startsWith / endsWith / indexOf / slice / substring /
  at / replace / replaceAll / split / 大小写 / trim / padStart / repeat / localeCompare）
  + 例 1 十五行实测；
- 专门提醒 emoji 与 <code>length</code> 的错位：实测
  <code>"👨‍👩‍👧".length → 8</code>、<code>[... "👨‍👩‍👧"].length → 5</code>，
  并说明“想按几个人算要用 <code>Intl.Segmenter</code>”；
- 正则入门：元字符表（<code>\d \w \s . [] [^] * + ? {n,m} ^ $ ( ) |</code> 与标志 <code>g i m u</code>）
  + 方法表（<code>test</code> / <code>exec</code> / <code>match</code> / <code>matchAll</code> /
  <code>replace</code> / <code>search</code>）+ 例 2 十行实测
  （手机号与邮箱校验、提取数字、具名捕获组、批量替换、捕获组重排、函数替换、matchAll、忽略大小写）；
- 正则的两条纪律（不要用正则解析 HTML；复杂正则别追求 100% 精确）；
- 数字：精度问题的成因、<code>Number.EPSILON</code> 比法、金额用“分”做整数运算、
  <code>toFixed</code> 的真实舍入行为（<code>(1.005).toFixed(2) → "1.00"</code> 及补丁写法）、
  <code>Number()</code> 与 <code>parseInt/parseFloat</code> 的差别、随机整数公式
  + 例 3 十八行实测（含 <code>Math.round(-2.5) → -2</code>、千分位与货币格式化）；
- 日期：六种创建方式的差别（本地 / ISO / 非 ISO / 时间戳 / 带偏移）、
  读取字段表（<code>getMonth()</code> 从 0、<code>getDay()</code> 从周日）、
  <code>toISOString</code> 与 <code>toLocale*</code> 的定位 + 例 4 十行实测
  （构造、星期、时间戳、ISO、本地化、Intl 自定义格式、加 45 天、日期相减得天数、ISO 解析的 UTC 行为）；
- 日期三个坑的总结（月份从 0、字符串解析有歧义、Date 自身可变）。

## 本批的自查

1. **把容易含糊的一行拆成两行**：emoji 那条原本只写“展开后的长度”，
   读者看不到 <code>.length</code> 到底是多少；改为同时给出 <code>8</code> 与 <code>5</code> 两个实测值，
   并补一句“按‘几个人’算要用 <code>Intl.Segmenter</code>”，避免半截知识。
2. **本地化输出用实测值校对**：<code>toLocaleDateString("zh-CN")</code>、
   <code>Intl.DateTimeFormat</code>、货币格式化这些依赖环境的结果，
   先跑出来（本例环境为 Asia/Shanghai）再决定正文怎么写；
   正文只承诺“格式长这样”，不承诺每个浏览器完全一致。
3. 四个演示的输出逐行读出，与正文里的代码块（如 <code>(1.005).toFixed(2)</code>）逐条对齐。

## 验证证据（2026-09-14）

- `python3 tools/check-demo-parity.py`：**75 页 / 262 个演示**全部一致（本式新增 4 个演示）；
- `python3 tools/check-demo-classes.py`：**35 个页面通过**；
- `python3 tools/check-links.py`：80 页内部链接全部有效；
- `bash tools/check-offline.sh`：无外网资源依赖；
- `python3 tools/gen-search-index.py`：索引更新为 79 页；
- CDP 双模式：34 式在 http:// 与 file:// 下渲染标记齐全、**零 JS 报错**；
- 演示断言（`cdp-eval.js`）节选：
  - 例 1：`"日拱一卒".length → 4`、`replace` 只换第一个（`a+b-c`）而 `replaceAll` 全换（`a+b+c`）、
    `"B".localeCompare("a") → 1`、emoji 的 8 与 5；
  - 例 2：手机号 `true/false`、提取数字 `["2026","0914","128"]`、
    具名组 `year = 2026 / month = 09 / day = 14`、重排 `14/09/2026`、
    函数替换「价格 6 和 10」、matchAll `["1","22","333"]`；
  - 例 3：`0.30000000000000004`、`(1.005).toFixed(2) → "1.00"`、
    补丁后 `"1.01"`、`Math.round(-2.5) → -2`、千分位 `1,234,567.891`、货币 `¥1,234.50`；
  - 例 4：本地时间构造 → `2026 年 9 月 14 日 9:30`、`getDay() = 1`、
    `toISOString() → 2026-09-14T01:30:00.000Z`、Intl 自定义格式 `2026年9月14日星期一`、
    加 45 天 → `2026/10/29`、日期相减约 17 天。
