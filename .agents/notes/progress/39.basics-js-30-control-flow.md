# 39 · 基础篇 30 式（控制流）

> 来源：2026-09-14 同一轮工作（基础篇改版的第二十二批次）。正文原样归档，编号只增不改。

## 本批完成的事

**`site/basics/30-js-control-flow.html` 控制流**（5 个演示 + 3 道练功）：

- <code>if / else if / else</code>：条件从具体排到宽泛、两个新手坑（<code>=</code> 写成判断、
  省掉大括号）+ 例 1 用成绩分级把“命中了哪条条件”也一起显示出来；
- <code>switch</code>：严格相等比较、<code>break</code> 的“贯穿”（fallthrough）、
  <code>default</code> 的位置不影响匹配顺序 + 例 2 同时演示
  “故意贯穿”（周六周日共用逻辑）与“忘了 break 的 bug”（周一把周二的功课也执行了）；
- 三种循环对照表（<code>for</code> / <code>while</code> / <code>do…while</code>）
  + 例 3：三条循环跑同一件事结果一致，再用“条件一开始就不成立”把
  <code>while</code>（0 次）与 <code>do…while</code>（1 次）的差别摆出来；
- 无限循环提醒（排查口诀：找回“让条件变化”的那一句）；
- <code>for…of</code> 与 <code>for…in</code> 对照表（拿值还是拿键、适合什么、坑在哪）
  + 例 4：数组的两种遍历（值 vs 字符串下标）、<code>entries()</code> 取下标、
  对象的 <code>Object.keys/entries</code>、字符串逐字遍历；
- <code>break</code> / <code>continue</code> / 标签表 + 例 5：找到 7 就停（并列出实际看过的数）、
  跳过偶数、带标签的 <code>break outer</code> 一次跳出两层；
- 「该用哪一个」速查表（范围判断 / 等值判断 / 二选一 / 已知次数 / 次数不定 / 至少一次 /
  遍历数组与对象）+ 一条**循环里 var 与 let** 的预告（第三十一式讲闭包）。

## 本批的自查

1. **写错引号被自己抓回**：例 2 的预览脚本里一处字符串引号写错
   （<code>say2(day + " → ' + plan(day) + '")</code>），会直接让整段脚本语法错误；
   与源码块对照时发现并改成正确写法，随后 parity 通过。
2. **对照演示与正文**：五个演示的输出逐行用 CDP 读出，与正文的结论一一核对，
   其中例 3 的“0 次 vs 1 次”、例 5 的“实际只看过 1–7”都写进了正文。
3. **补一句解构的预告**：例 4 用到 <code>for (const [i, fruit] of fruits.entries())</code>，
   这在语法上属于解构（第三十二式才讲），因此在正文注明“照着读成第 i 项是 fruit 即可”，
   避免读者卡在一个还没学的语法上。

## 验证证据（2026-09-14）

- `python3 tools/check-demo-parity.py`：**71 页 / 243 个演示**全部一致（本式新增 5 个演示）；
- `python3 tools/check-demo-classes.py`：**31 个页面通过**；
- `python3 tools/check-links.py`：76 页内部链接全部有效；
- `bash tools/check-offline.sh`：无外网资源依赖；
- `python3 tools/gen-search-index.py`：索引更新为 75 页；
- CDP 双模式：30 式在 http:// 与 file:// 下渲染标记齐全、**零 JS 报错**；
- 演示断言（`cdp-eval.js`）节选：
  - 例 1：95→优秀 / 80→良好 / 61→及格 / 59 与 0→需要再练（并标出命中的条件）；
  - 例 2：周六与周日都得到“休息与复习”；漏写 break 的版本里
    “周一 → '抄写骨架 读第二式'”；
  - 例 3：三条循环结果均为 `1 2 3 4 5`；条件不成立时 <code>while</code> 执行 0 次、
    <code>do…while</code> 执行 1 次（值是 10）；
  - 例 4：`for…in` 数组得到 `0:苹果、1:橘子、2:梨`；
    `for…of` 字符串得到 `日 / 拱 / 一 / 卒`；
  - 例 5：第一个能被 7 整除的数是 7、实际只看过 1–7；跳过偶数得到 1、3、5、7、9；
    带标签的 break 停在 `2-2`。
