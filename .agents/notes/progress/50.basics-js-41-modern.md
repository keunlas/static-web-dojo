# 50 · 基础篇 41 式（现代 JavaScript）

> 来源：2026-09-14 同一轮工作（基础篇改版的第三十三批次）。正文原样归档，编号只增不改。

## 本批完成的事

**`site/basics/41-js-modern.html` 现代 JavaScript**（3 个演示 + 3 道练功 + 3 个结构块）：

- 「ES6+ 总览」：十二行特性表（特性 / 出现版本 / 一句话 / **本书学过的地方**）——
  <code>let·const</code>、箭头函数、模板字符串、解构与展开、默认参数与剩余参数、
  <code>class</code>、模块、<code>Promise</code>（ES2015）、<code>async/await</code>（ES2017）、
  <code>?.</code> 与 <code>??</code>（ES2020）、逻辑赋值（ES2021）、类字段与私有字段（ES2022）；
  **例 1** 新语法尝鲜盘：五行输出把模板字符串、对象/数组解构与剩余、
  箭头函数与默认参数、<code>??</code>、可选链、<code>reduce</code>、<code>class</code> 全跑一遍；
- 「模块：把代码分成几家人」：ES 模块的 <code>export/import</code> 写法结构块 +
  警示框说明**本站为什么不用模块**（<code>type="module"</code> 在 file:// 下被 CORS 拦，
  铁律是“双击即开、零构建”，于是改为经典脚本按顺序引入 + <code>window</code> 命名空间，
  本站的 <code>DOJO</code> 就是这么来的）；**例 2** 用
  <code>window.DOJO41_TOOLS</code> 演示“挂命名空间”这条出口，
  并顺带演示 <code>[...text].length</code> 数中文的正确姿势；
- 「兼容性：先检测，再决定」：特性检测与降级的结构块 + 四行对照表
  （特性检测 / 降级 / polyfill 补 API / 转译补语法各自的代价）；
  **例 3** 现场体检十二项（Promise、async-await、<code>?.</code>、<code>??</code>、
  fetch、IntersectionObserver、structuredClone、<code>Array.at</code>、
  <code>replaceAll</code>、CSS 网格、CSS <code>min()</code>、localStorage），
  其中**新语法用 <code>new Function("…")</code> 包成字符串**再检测——
  老浏览器不会因为解析不了而在第一行就报错，这本身就是一句该记住的实践；
- 「继续往哪走」：三条路的小地图（更稳 / 更顺 / 更大）+ 查文档先看
  “语法、返回值、浏览器支持”三栏 + 一个 MDN 文字外链 + 心法
  “语言每年都在长，但基本功长得慢”；
- 本式小结六条 + 练功三题（新旧写法对照重写、给自己的页面加两处检测与降级、
  思考本站不用模块换来了什么又失去了什么）。

## 本批的自查

1. **抓到并修掉一处离线扫描误伤**：例 3 的体检清单里原本有一项字符串
   <code>'fetch()'</code>——它出现在**真实脚本**里，于是
   `tools/check-offline-tech.py` 报“脚本体内调用了 fetch()”，整站
   `check-offline.sh` 判失败。改成 `'fetch 网络请求'` 后通过。
   **记录约定：真实脚本里要提到被禁 API 时，别写成带括号的调用样子**
   （哪怕只是字符串），否则会被脚本感知扫描拦下；正文与教学源码块（text/plain）不受影响。
2. 三个演示断言（http:// 与 file:// 各跑一遍）：
   - 例 1：五行输出与设计一致——
     「模板字符串 + 解构：墨白 的头一门手艺是 剑法，其余 2 门是 茶道、木工」、
     「pick(skills, 9) = （没有这一项）」、「config.font?.size = 未设置」、
     「reduce 求和 = 10」、「class：画一笔朱砂」；
   - 例 2：点击后 <code>wrap("静心") = 《静心》</code>、<code>count("静心") = 2</code>；
   - 例 3：headless Chromium 下十二项全部“支持”，页尾一句
     「这就是特性检测：不猜，直接问浏览器。」；
3. **人工目检**：<code>cdp-shot.js</code> 截例 3 区块，体检清单与源码块版式正常。

## 验证证据（2026-09-14）

- `python3 tools/check-demo-parity.py`：**82 页 / 293 个演示**全部一致（本式新增 3 个演示）；
- `python3 tools/check-demo-classes.py`：**42 个页面通过**；
- `python3 tools/check-links.py`：87 页内部链接全部有效；
- `bash tools/check-offline.sh`：无外网资源依赖（修掉例 3 那处误伤后通过；
  本页有一个允许的 MDN 文字外链，已在第 3 项列出供人工核对）；
- `python3 tools/gen-search-index.py`：索引更新为 86 页；
- CDP 双模式：第 41 式在 http:// 与 file:// 下渲染标记齐全
  （sidebar 78 项、codeblocks 10、toc、pager 2）、**零 JS 报错**。

## 补充（2026-09-14 全站终验的“查缺补漏”小批次）

- 终验扫全站关键词时发现：全文没有任何一处讲 **Web Animations API**
  （<code>Element.prototype.animate()</code>）——而它是“用 JS 写动画”的正门，
  与本式“现代 JavaScript 有哪些能力”的主题正相配。已在「兼容性」一节末尾补一段
  **「Element.animate()：JS 版的关键帧（进阶参考）」**：一段带特性检测与降级的代码块，
  说明它与第二十四式的时长/缓动/循环概念一一对应，并给出分工结论
  （参数要由 JS 算出来时用它；纯装饰动画仍写在 CSS 里）。
- 复验：`check-demo-parity.py`（3 页 11 个演示）✔、`check-demo-classes.py` ✔、
  `check-offline.sh` ✔（本页唯一的外链是允许的 MDN 文字链）、`check-links.py` 90 页 ✔；
  CDP：本式 codeblocks 由 10 增至 11、h1「现代 JavaScript」、**零 JS 报错**。
