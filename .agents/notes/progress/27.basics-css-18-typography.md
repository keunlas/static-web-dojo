# 27 · 基础篇 18 式（排版与字体）

> 来源：2026-09-14 同一轮工作（基础篇改版的第十批次）。正文原样归档，编号只增不改。

## 本批完成的事

**`site/basics/18-css-typography.html` 排版与字体**（5 个演示 + 3 道练功）：

- 字体栈：通用族表（serif / sans-serif / monospace / system-ui / cursive·fantasy）、
  “名字带空格要加引号、通用族不能加引号”两条细节、中文三平台（苹方 / 微软雅黑 / Noto）的排法；
  例 1 现场对比无衬线栈、衬线栈与等宽栈；
- <code>@font-face</code>：登记写法（<code>src</code> 多格式兜底、<code>font-weight</code> 区间、
  <code>font-display: swap</code>）+ <code>font-display</code> 取值表 +
  「离线项目优先系统字体」的心法（中文字体体积与加载闪烁两个代价）；
- 字号 / 字重 / 样式表（<code>font-size</code>、<code>font-weight</code> 100–900、
  <code>font-style</code>、<code>font-synthesis</code>）+ 例 2 七档字重一行排开
  （“系统字体多半只有 400/700 两档”）；<code>font</code> 简写与它会重置未写属性的坑；
- 行高三种写法对照表 + 例 3：1.2 / 1.7 / 2.2 的阅读对比，以及**大字下无单位行高会重算**的现场；
  <code>letter-spacing</code> / <code>word-spacing</code> 的用法；
- 文本属性速查表（text-align / text-indent / text-decoration / text-transform / text-shadow /
  white-space / overflow-wrap / word-break / text-overflow / vertical-align / writing-mode）
  + 例 4（波浪下划线、删除线、阴影、字距、首行缩进）；
- 换行与省略号：单行省略号“三条属性配方” + 例 5（单行省略、<code>-webkit-line-clamp</code> 两行截断、
  长网址 <code>overflow-wrap: break-word</code> 不撑破盒子），并说明
  “省略号意味着信息被藏起来，重要内容要留看全的入口”；
- 中文排版六条硬经验（字号下限、行高区间、每行 30–40 字、长文别居中、标点与中英混排要统一、
  慎用细体字）与小结、练功（含 <code>line-height: 1.7</code> vs <code>1.7em</code> 的思考题）。

## 验证证据（2026-09-14）

- `python3 tools/check-demo-parity.py`：**59 页 / 189 个演示**全部一致（本式新增 5 个演示）；
- `python3 tools/check-links.py`：63 页内部链接全部有效；
- `bash tools/check-offline.sh`：无外网资源依赖；
- `python3 tools/gen-search-index.py`：索引更新为 62 页；
- CDP：18 式在 file:// 下渲染标记齐全（`codeblocks:16`）、**零 JS 报错**；
- 计算样式断言（`cdp-eval.js`）：
  - 例 3：16px 字号下 1.2 / 1.7 / 2.2 分别算出 19.2 / 27.2 / 35.2px；
    1.6rem（25.6px）的加大字块行高为 43.52px（= 1.7 × 25.6），坐实“无单位行高会重算”；
  - 例 4：波浪下划线四项（line/style/color/thickness）全部生效；
    字距 2.88px（.18em × 16px）、首行缩进 32px（2em）、阴影字符串符合预期；
  - 例 5：单行块 `scrollWidth 393 > clientWidth 230` 且 `text-overflow: ellipsis`、`white-space: nowrap`；
    多行块 `-webkit-line-clamp: 2` 生效（内容高 114px、可视 63px，两行截断）；
    长网址块**不溢出**（`overflowing: false`，`overflow-wrap: break-word`）；
  - 例 1：三段文字的计算字体栈与写法逐字一致；例 2：七档字重计算值 100–900 均在。
- 人工目检：截取例 3、例 5 局部大图，确认省略号、两行截断与长网址换行在纸墨风下显示正常。
