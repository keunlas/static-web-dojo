# 28 · 基础篇 19 式（背景、边框与阴影）

> 来源：2026-09-14 同一轮工作（基础篇改版的第十一批次）。正文原样归档，编号只增不改。

## 本批完成的事

**`site/basics/19-css-background-border.html` 背景、边框与阴影**（5 个演示 + 3 道练功）：

- 背景全家桶表（颜色 / 图 / 重复 / 位置 / 尺寸 / 裁切 / 附着）+ 例 1：
  同一张图三种 <code>background-size</code>（cover 裁边、contain 留白、100% 100% 变形）；
- 多重背景的套路（渐变罩在图片上，先写的在上层，附结构块）；
- 三种渐变对照表（linear / radial / conic）+ 例 2 四个色块（含 repeating 条纹），
  并说明两个实用细节：<code>transparent</code> 灰边问题的替代写法、双位置色标做硬边；
- 边框三要素表（含“只写 width 不写 style 等于没边框”这个头号坑）、单边边框、
  <code>border-image</code>、<code>outline</code> 与 <code>border</code> 的区别；
- 圆角三档写法块（<code>10px</code> 卡片 / <code>999px</code> 胶囊 / <code>50%</code> 正圆 / 斜杠椭圆角）+ 例 3 六格对照；
- <code>box-shadow</code> 五个参数与常用写法表 + 例 4 四种阴影（单层 / 多层 / 带主色 / 内阴影）；
- 「阴影要克制、方向一致、别用纯黑、动画优先做 transform」四条心法；
- <code>background-clip: text</code> 渐变文字 + 例 5（渐变标题与胶囊标签），
  并配一条**可访问性提醒**：文字被裁成图案后对比度取决于图案本身。

## 验证证据（2026-09-14）

- `python3 tools/check-demo-parity.py`：**60 页 / 194 个演示**全部一致（本式新增 5 个演示）；
- `python3 tools/check-links.py`：64 页内部链接全部有效；
- `bash tools/check-offline.sh`：无外网资源依赖；
- `python3 tools/gen-search-index.py`：索引更新为 63 页；
- CDP：19 式在 file:// 下渲染标记齐全（`codeblocks:15`）、**零 JS 报错**；
- 计算样式断言（`cdp-eval.js`）：
  - 例 1：三个盒子的 <code>background-size</code> 分别为 cover / contain / 100% 100%，
    背景图指向 <code>demo-wide.svg</code>、`no-repeat` + 居中；
  - 例 2：线性 / 径向 / 锥形 / 重复线性四种渐变都算出预期表达式，径向块另有底色兜底；
  - 例 3：只写宽度的方块 `border-top-style: none` 且计算宽度为 **0px**（证实“没样式就没边框”）；
    solid 为 3px、胶囊圆角 999px、正圆为 50% 且实际尺寸 66×66（确实是圆）；
  - 例 4：四种阴影字符串与写法逐字对应（含 `inset`）；
  - 例 5：`background-clip: text` 生效、文字色为 `rgba(0,0,0,0)`、背景是朱砂→蓝的线性渐变。
- 人工目检：截取例 3、例 5 局部大图，确认边框样式、胶囊/正圆与渐变文字在纸墨风下显示正常。

## 备注

- 本章演示复用了既有本地素材（<code>assets/img/demo-wide.svg</code>）作为背景图，
  没有新增文件；这也让“背景图”演示依然保持离线。
