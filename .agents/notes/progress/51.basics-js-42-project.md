# 51 · 基础篇 42 式（综合修炼：让个人主页动起来）

> 来源：2026-09-14 同一轮工作（基础篇改版的第三十四批次）。正文原样归档，编号只增不改。

## 本批完成的事

**新成品页 `site/demo/html-home-interactive/index.html`（动起来版）**：

- 结构与第二十六式的「穿上衣服版」一致，样式沿用它的设计令牌，
  **只多了一个 `<script>` 与几个 `id` / `data-kind` 标记**；
- 五个行为：主题切换（`data-theme` + CSS 变量 + `localStorage` + `aria-pressed`）、
  作品筛选（`data-kind` + `hidden` + `role="status"` 结果条）、
  留言板（拦提交 + 校验 + `textContent` + `storage` + 事件委托删除）、
  回到顶部（`scroll` + 150ms 节流 + `scrollTo({behavior:"smooth"})` + `hidden`）、
  滚动高亮导航（`IntersectionObserver` + `rootMargin` 窄缝 + `aria-current`）；
- 独立页补齐 `<link rel="icon">`（内嵌 SVG data URI），file:// 与 http:// 下控制台都干净。

**`site/basics/42-js-project.html` 综合修炼章（0 个演示，沿用第二十六式的成品拆解写法）**：

- 「成品先看」：三个版本的链接并排（纯 HTML 版 / 穿衣服版 / 动起来版），
  点明“HTML 与 CSS 几乎一字未改，行为是叠加上去的”；
- 「动手拆解」七段：第 0 步先列**行为清单表**（要的效果 → 用到的本事 → 学过的地方）；
  第 1 步主题切换（CSS 变量覆盖块 + 带 `try/catch` 的读写存储）；
  第 2 步作品筛选（筛选栏 HTML + `hidden` 切换）；
  第 3 步留言板（完整脚本，含 `unshift`、重渲染、委托删除、`Number(dataset.index)`）；
  第 4 步回到顶部（节流 + `[hidden]` 的 CSS 兜底 + 减少动效）；
  第 5 步滚动高亮（`IntersectionObserver` 全段）；
  收尾说明脚本为什么放 `</body>` 之前；
- 「对照验收清单」七条：关掉 JS 仍可读、键盘能走完、用户输入只走 `textContent`、
  无行内事件、刷新后记忆还在、file:// 与 http:// 双开无红字、动效照顾
  `prefers-reduced-motion`；
- 本式小结六条 + 练功三题（日志按日期正倒序、留言只留最近十条 + 相对时间、
  思考主题切换为什么用 dataset + CSS 变量而不是逐个元素改颜色）。

## 本批的自查

1. **成品页五个行为逐条断言**（http:// 与 file:// 各跑一遍）：
   - 主题：点击后 `html[data-theme="night"]`、`localStorage["home-theme"]="night"`、
     `aria-pressed="true"`、按钮文案变「☀ 亮一点」；
   - 筛选：点「表单」后 `显示 1 / 3 件作品`、可见卡片只剩「报名表单」；
   - 留言：提交后 `msg-status` 变「收下了」，列表出现一条，
     `localStorage` 里是 `[{"name":…,"text":…,"at":"2026-09-14"}]`；点删除后列表回到
     「（还没有口信）」、存储变 `[]`；
   - 回到顶部：滚动后 `hidden` 变 `false`；
   - 高亮：滚动后 `.site-nav a.is-current` 命中当前小节。
2. **成品页的 404 排查**：首次断言时控制台有一条 `Failed to load resource: 404`——
   独立页不走 loader、没有 favicon，浏览器去要 `/favicon.ico` 落空。
   按第十式的老坑给成品页补了内嵌 SVG 图标，复验两种模式都“错误：无”
   （现存的 `demo/html-home`、`demo/html-home-styled` 两页仍有这个历史小尾巴，
   不影响使用，留给日后的清理批次）。
3. **章节页 0 演示**：综合修炼章按约定以“成品 + 拆解 + 清单”为主，
   `check-demo-parity.py` 对本式为 0 个演示、正常通过。
4. **人工目检**：`cdp-shot.js` 截成品页顶部——主题按钮、高亮中的导航、
   筛选栏与卡片版式都正常。

## 验证证据（2026-09-14）

- `python3 tools/check-demo-parity.py`：**83 页 / 293 个演示**全部一致（本式新增 0 个演示）；
- `python3 tools/check-demo-classes.py`：**43 个页面通过**；
- `python3 tools/check-links.py`：89 页内部链接全部有效
  （含章节页 ↔ 成品页互链：`basics/42-js-project.html` ↔ `demo/html-home-interactive/`）；
- `bash tools/check-offline.sh`：无外网资源依赖；
- `python3 tools/gen-search-index.py`：索引更新为 88 页；
- CDP 双模式：章节页渲染标记齐全（sidebar 79 项、codeblocks 9、toc、pager 2）、
  成品页（独立页，无侧栏/顶栏属正常）**两个页面在两种模式下均零 JS 报错**。
