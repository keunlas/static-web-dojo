# 40 · 基础篇 31 式（函数与作用域）

> 来源：2026-09-14 同一轮工作（基础篇改版的第二十三批次）。正文原样归档，编号只增不改。

## 本批完成的事

**`site/basics/31-js-functions.html` 函数与作用域**（5 个演示 + 3 道练功）：

- 三种写法对照表（函数声明 / 函数表达式 / 箭头函数）与选型建议
  （要 <code>this</code> 用 <code>function</code>，做回调用箭头）；
- 例 1 **提升**：函数声明可以“提前上岗”，函数表达式在赋值前
  <code>typeof</code> 得到 <code>"undefined"</code>（用 <code>var</code> 才能安全地问，
  <code>const</code> 会撞暂时性死区）；
- 参数与返回值：默认参数（只有 undefined 才触发）、rest 参数取代
  <code>arguments</code>、守卫式提前 <code>return</code>、无返回值即 <code>undefined</code>
  + 例 2 逐项实测（含“传空字符串不走默认值”）；
- 作用域三层（全局 / 函数 / 块级）+ 就近查找与同名遮蔽 + “别污染全局”；
  例 3 用三层同名变量把结论钉死；
- 闭包：定义（函数记得出生时的环境）+ 两个经典用途（计数器 / 循环里记住自己的 i）
  + 例 4 实测：计数器 1→2→3、两个计数器互不干扰、函数外 <code>typeof count</code> 为
  <code>undefined</code>（私有变量），以及**用 <code>var</code> 的循环得到 3、3、3，
  用 <code>let</code> 得到 0、1、2**；
- 闭包的代价（内存占用）提醒，并预告第三十八式事件绑定会再遇到；
- 递归（终止条件、栈溢出风险）与回调 / 高阶函数 + 例 5
  （阶乘 5! 与 10!、递归倒计时、<code>map</code> 传回调、自定义 <code>pick(list, test)</code>）；
- <code>this</code> 初步三条规则表（方法调用 / 直接调用在严格模式下是 undefined /
  箭头函数用外层 this）+ 结构块示例，并注明第三十五、三十八式会展开。

## 本批的自查

1. **抓出一处“预览会报错”的顺序问题**：例 1 的预览里我先调用
   <code>arrowed('墨白')</code>、后写下 <code>var arrowed = …</code>——
   运行时会抛 <code>TypeError: arrowed is not a function</code>，
   既污染控制台又会中断后面的脚本；与源码块对照后改正顺序（这正是“函数表达式不提升”的活教材，
   但演示本身必须跑得干净）。修好后 CDP 复验：**零 JS 报错**。
2. **同名变量排查**：五个演示的脚本共享同一个页面全局作用域，
   逐个核对了顶层变量名不重复（<code>list1…list5</code>、<code>say1…say5</code>、
   <code>who</code>、<code>next</code> 等），避免“两个演示互相踩变量”。
3. 五个演示的输出逐行读出，正文里的每个结论（尤其 3、3、3 与 0、1、2）都来自实测。

## 验证证据（2026-09-14）

- `python3 tools/check-demo-parity.py`：**72 页 / 248 个演示**全部一致（本式新增 5 个演示）；
- `python3 tools/check-demo-classes.py`：**32 个页面通过**；
- `python3 tools/check-links.py`：77 页内部链接全部有效；
- `bash tools/check-offline.sh`：无外网资源依赖；
- `python3 tools/gen-search-index.py`：索引更新为 76 页；
- CDP 双模式：31 式在 http:// 与 file:// 下渲染标记齐全、**零 JS 报错**；
- 演示断言（`cdp-eval.js`）节选：
  - 例 1：函数声明提前调用成功；<code>typeof expressed → undefined</code>；赋值后两者都可调用；
  - 例 2：<code>greet2()</code> 与 <code>greet2(undefined)</code> 都得“无名氏”，
    <code>greet2("")</code> 保留空串；<code>sum2(1,2,3,4) = 10</code>、<code>sum2() = 0</code>；
    <code>formatPrice2</code> 三种输入分别得“价格未知 / 价格不能为负 / ¥12.50”；
  - 例 4：<code>计数器一：1 → 2 → 3</code>、<code>另一个计数器：1</code>、
    <code>typeof count → undefined</code>、var 循环 <code>3、3、3</code>、let 循环 <code>0、1、2</code>；
  - 例 5：<code>5! = 120，10! = 3628800</code>、倒计时 <code>5 → 4 → 3 → 2 → 1</code>、
    高阶函数筛出“大于 4 的 8、12”与“奇数 3、1”。
